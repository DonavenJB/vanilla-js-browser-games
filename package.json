{
    "name": "vanilla-js-browser-games",
    "private": true,
    "type": "module",
    "scripts": {
        "test": "node --test",
        "audit": "node scripts/audit.mjs",
        "check": "npm run audit && npm test"
    }
}
